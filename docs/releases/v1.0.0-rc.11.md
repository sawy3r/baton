# Baton v1.0.0-rc.11 — publication recovery

RC11 republishes the exact RC10 behavior under a fresh immutable release
identity. It adds no protocol, operation, receipt, schema, board, or
record-engine behavior.

RC10 remains a valid immutable code tag. Its empty GitHub release was removed
before the archive and checksum were attached; GitHub permanently prevents a
deleted immutable release tag from being published again. RC11 carries the
same structural record reservation, truthful standalone board projection,
current-plan handoff guidance, and macOS system-Git portability under an
identity that can be published completely.

## Payload

The five standalone skills are generated deterministically from the canonical
RC11 operations. Their payload digest is:

```text
sha256:f3125e25d85f13cbab5437cb52a61627be33775d4f46b5665d4976b94cba12cc
```

## Publication

Create the GitHub prerelease as a draft, attach the reproducible archive and
checksum, verify both downloaded assets, and only then publish it. The RC10 tag
is never moved or reused.
